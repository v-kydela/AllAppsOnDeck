<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.AllAppsOnDeck" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">?attr/isLightTheme</item>
    </style>

    <style name="Theme.AllAppsOnDeck" parent="Base.Theme.AllAppsOnDeck" />

    <style name="Theme.AllAppsOnDeck.Transparent" parent="Theme.AllAppsOnDeck">
        <item name="android:windowShowWallpaper">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

</resources>